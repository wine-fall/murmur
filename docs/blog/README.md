Engineering notes from building murmur: how a piece of it works, with the numbers we measured while building it.
Files are named `YYYY-MM-slug.md` (the month the post was written, then a short slug).
Every number in a post cites its primary source in this repo: a spec section, a PR number, a log, or a script.
